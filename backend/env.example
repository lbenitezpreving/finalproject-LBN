# Base de datos
DATABASE_URL="postgresql://postgres:password@localhost:5432/taskdistributor"

# JWT para autenticación
JWT_SECRET="your-super-secret-jwt-key"
JWT_EXPIRE="30d"

# Configuración del servidor
PORT=4000
NODE_ENV=development

# Configuración de Redmine API (futuro uso)
REDMINE_BASE_URL="https://your-redmine-server.com"
REDMINE_API_KEY="your-redmine-api-key"

# Email configuration (futuro uso para alertas)
EMAIL_FROM="taskdistributor@yourcompany.com"
SMTP_HOST="smtp.yourcompany.com"
SMTP_PORT=587
SMTP_USERNAME="your-smtp-username"
SMTP_PASSWORD="your-smtp-password"

# Configuración de Supabase (si usas Supabase)
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key" 